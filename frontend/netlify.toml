[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Only redirect non-existent routes to index.html (for React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200